import React from 'react';
import { View, Text } from 'react-native';

// STUB: Safe Dashboard (New File)
// This file name has never existed, so it cannot be cached.
export default function SafeDashboard() {
    return (
        <View style={{ flex: 1, backgroundColor: 'orange', padding: 50 }}>
            <Text style={{ fontSize: 24 }}>SAFE DASHBOARD WORKS (ORANGE)</Text>
            <Text>The old WebDashboardScreen.tsx was cached/cursed.</Text>
        </View>
    );
}
