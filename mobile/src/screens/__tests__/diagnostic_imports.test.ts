
import RoutineScreen from '../RoutineScreen';

describe('Diagnostic Import Tests', () => {
    it('should import RoutineScreen without crashing', () => {
        expect(RoutineScreen).toBeDefined();
    });
});
